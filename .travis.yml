language: node_js
node_js: 14.16.0

jdk:
  - oraclejdk8
addons:
  sonarcloud: true
script:
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN -Dsonar.organization=alejozavala -Dsonar.projectKey=alejozavala_IngSW-CICD -Dsonar.sources='./app' -X